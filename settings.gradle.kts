pluginManagement {
    repositories {
        gradlePluginPortal()
        // Temporary until initial release
        maven("https://repo.spongepowered.org/repositories/maven-public/") {
            name = "sponge"
        }
    }
}